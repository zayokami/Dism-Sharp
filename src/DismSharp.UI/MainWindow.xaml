<ui:FluentWindow
    x:Class="DismSharp.UI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:views="clr-namespace:DismSharp.UI.Views"
    Title="Dism#"
    Width="1100"
    Height="700"
    WindowStartupLocation="CenterScreen"
    WindowBackdropType="Mica"
    ExtendsContentIntoTitleBar="True">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="Dism#" />

        <ui:NavigationView
            x:Name="RootNavigation"
            Grid.Row="1"
            PaneDisplayMode="Left"
            IsBackButtonVisible="Collapsed">

            <ui:NavigationView.MenuItems>
                <ui:NavigationViewItem Content="仪表盘" Icon="{ui:SymbolIcon Desktop24}" TargetPageType="{x:Type views:DashboardPage}" />
                <ui:NavigationViewItem Content="系统清理" Icon="{ui:SymbolIcon Delete24}" TargetPageType="{x:Type views:CleanupPage}" />
                <ui:NavigationViewItem Content="Windows 功能" Icon="{ui:SymbolIcon Settings24}" TargetPageType="{x:Type views:FeaturesPage}" />
                <ui:NavigationViewItem Content="更新包管理" Icon="{ui:SymbolIcon ArrowDownload24}" TargetPageType="{x:Type views:PackagesPage}" />
                <ui:NavigationViewItem Content="驱动管理" Icon="{ui:SymbolIcon DeviceMeetingRoom24}" TargetPageType="{x:Type views:DriversPage}" />
                <ui:NavigationViewItem Content="Appx 管理" Icon="{ui:SymbolIcon Apps24}" TargetPageType="{x:Type views:AppxPage}" />
                <ui:NavigationViewItem Content="启动项" Icon="{ui:SymbolIcon Power24}" TargetPageType="{x:Type views:BootPage}" />
                <ui:NavigationViewItem Content="右键菜单" Icon="{ui:SymbolIcon CursorHover24}" TargetPageType="{x:Type views:ContextMenuPage}" />
                <ui:NavigationViewItem Content="映像管理" Icon="{ui:SymbolIcon Image24}" TargetPageType="{x:Type views:ImagePage}" />
            </ui:NavigationView.MenuItems>

            <ui:NavigationView.FooterMenuItems>
                <ui:NavigationViewItem Content="关于" Icon="{ui:SymbolIcon Info24}" TargetPageType="{x:Type views:AboutPage}" />
            </ui:NavigationView.FooterMenuItems>

        </ui:NavigationView>
    </Grid>
</ui:FluentWindow>
